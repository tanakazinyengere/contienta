import { createClippedInConfig } from "ClippedIn-agent-playwright-config/config";

export default createClippedInConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});

